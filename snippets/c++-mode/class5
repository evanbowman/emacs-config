# -*- mode: snippet -*-
# name: class5
# key: class5
# --
class ${1:NAME} {
public:
    $1() = default;
    $1($1& other) = default;
    $1($1&& other) = default;
    $1& operator=($1& other) = default;
    $1& operator=($1&& other) = default;
    ~$1() = default;

private:
    $0
};